name: VKSelfCode
options:
  bundleIdPrefix: dev.selfcode
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  groupSortPosition: top

packages:
  VKID:
    url: https://github.com/VKCOM/vkid-ios-sdk.git
    from: 2.2.0

settings:
  base:
    SWIFT_VERSION: 5.0
    SWIFT_STRICT_CONCURRENCY: minimal
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
    TARGETED_DEVICE_FAMILY: 1
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: 0.1.0
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic

configs:
  Debug: debug
  Release: release

targets:
  VKSelfCode:
    type: application
    platform: iOS
    sources:
      - path: VKSelfCode
        excludes:
          - Resources
    resources:
      - path: VKSelfCode/Resources/Assets.xcassets
    info:
      path: VKSelfCode/Resources/Info.plist
    configFiles:
      Debug: Config/Secrets.xcconfig
      Release: Config/Secrets.xcconfig
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.selfcode.vk
        PRODUCT_NAME: VKSelfCode
        INFOPLIST_FILE: VKSelfCode/Resources/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_USER_SCRIPT_SANDBOXING: YES
    dependencies:
      - package: VKID
        product: VKID
    scheme:
      testTargets: []
      gatherCoverageData: false
